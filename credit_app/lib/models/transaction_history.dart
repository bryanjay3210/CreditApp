class TransactionHistory {
  late String creditor;
  late DateTime dateStmp;
  late double amount;
  late bool isCredit;

  TransactionHistory(this.creditor, this.dateStmp, this.amount, this.isCredit);
}
